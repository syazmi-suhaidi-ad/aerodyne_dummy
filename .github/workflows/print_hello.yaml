name: Hello World Workflow

on:
  workflow_dispatch:
    inputs:
      trigger:
        description: 'Trigger manually'
        default: 'true'

jobs:
  hello_world_job:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "Hello, World!"
